# Two-terminal demo — run of show
LEFT (member): `cd ~/clout && claude` →
  1. "find my top contacts"  → capability card → approve → top-5 professional → approve adds
  2. (right terminal posts the ask) → "check my clout inbox" → THE CARD → approve/refer
  3. `cat ~/clout/ledger/ledger.md` — the audit trail
RIGHT (Clout agent): main workspace claude →
  1. "intake the solid GTM ask and post it": cp demo/ask-solid-gtm.json ~/clout/inbox/
  2. after member responds: read ~/clout/outbox/ask-001-response.json → assemble
Close: "Nothing left this laptop. The member saw every question. The ledger proves it."
